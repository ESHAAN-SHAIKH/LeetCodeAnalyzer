---
interface Props {
  slotId?: string;
  adFormat?: string;
  responsive?: boolean;
  style?: string;
  className?: string;
}

const {
  slotId = "1234567890", // default placeholder slot ID
  adFormat = "auto",
  responsive = true,
  style = "display:block",
  className = "",
} = Astro.props;
---

<div class={`ad-container my-6 rounded-xl border border-surface-600 bg-surface-800/30 p-4 text-center backdrop-blur-sm ${className}`}>
  <!-- TODO: Replace with your actual AdSense publisher ID (e.g. ca-pub-XXXXXXXXXXXXXXXX) and slot ID -->
  <!-- data-ad-client="ca-pub-XXXXXXXXXXXXXXXX" -->
  <!-- data-ad-slot={slotId} -->
  <div class="ad-placeholder flex flex-col items-center justify-center py-8 px-4 border border-dashed border-surface-600 rounded-lg bg-surface-900/40">
    <span class="text-xs font-semibold uppercase tracking-widest text-ink_text-muted mb-1 font-sans">Sponsored Advertisement</span>
    <span class="text-[10px] text-ink_text-muted/80 font-mono">AdSlot ID: {slotId}</span>
    
    <ins class="adsbygoogle"
         style={style}
         data-ad-client="ca-pub-placeholder"
         data-ad-slot={slotId}
         data-ad-format={adFormat}
         data-full-width-responsive={String(responsive)}></ins>
    <script is:inline>
         (adsbygoogle = window.adsbygoogle || []).push({});
    </script>
  </div>
</div>
